export type FullName = {
    firstName: string
    lastName: string
}

export type BookInfo = {
    title: string
    author: string
    date: string
}

export type PieceInfo = {
    composer: string
    piece: string
    genre: string
}